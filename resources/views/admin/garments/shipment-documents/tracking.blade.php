@extends('auto_posts.admin.layouts.admin')
@push('title') {{ $title }} @endpush
@section('content')
<div class="section-title"><h2 class="title">{{ __($title) }} - {{ $shipment->document_number }}</h2></div>
<div class="row g-4"><div class="col-xl-5"><div class="section-wrap p-4"><h4 class="mb-3">{{ __('Add Tracking Event') }}</h4><form method="post" action="{{ route('admin.garments.shipment-documents.tracking.store', $shipment->id) }}">@csrf<div class="form-group mb-3"><label class="form-label">{{ __('Status') }}</label><input class="form-control" name="status" placeholder="{{ __('In Transit, Arrived, Customs Cleared') }}" required></div><div class="form-group mb-3"><label class="form-label">{{ __('Location') }}</label><input class="form-control" name="location"></div><div class="form-group mb-3"><label class="form-label">{{ __('Event Time') }}</label><input class="form-control" type="datetime-local" name="event_at" required></div><div class="form-group mb-3"><label class="form-label">{{ __('Notes') }}</label><textarea class="form-control" name="notes"></textarea></div><button class="primary-btn" type="submit">{{ __('Add Event') }}</button></form></div></div><div class="col-xl-7"><div class="section-wrap p-4"><h4 class="mb-4">{{ __('Tracking Timeline') }}</h4>@forelse($events as $event)<div class="border-start border-3 border-primary ps-3 pb-4"><h5 class="mb-1">{{ $event->status }}</h5><p class="mb-1 text-muted">{{ $event->location ?: __('Location not provided') }}</p><small>{{ $event->event_at?->format('d M Y, h:i A') }}</small>@if($event->notes)<p class="mt-2 mb-0">{{ $event->notes }}</p>@endif</div>@empty<p class="text-muted">{{ __('No tracking events found') }}</p>@endforelse</div></div></div>
@endsection

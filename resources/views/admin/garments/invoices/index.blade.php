@extends('auto_posts.admin.layouts.admin')
@push('title') {{ $title }} @endpush
@section('content')
<div class="section-title"><h2 class="title">{{ $title }}</h2><button class="primary-btn" data-bs-toggle="modal" data-bs-target="#invoice-modal"><i class="fa fa-plus me-2"></i>{{ __('Create Invoice') }}</button></div>
<div class="section-wrap p-4"><div class="table-waraper"><table class="display primary-table"><thead><tr><th>{{ __('Invoice') }}</th><th>{{ __('Order') }}</th><th>{{ __('Buyer') }}</th><th>{{ __('Issue Date') }}</th><th>{{ __('Due Date') }}</th><th>{{ __('Total') }}</th><th>{{ __('Paid') }}</th><th>{{ __('Status') }}</th><th>{{ __('Action') }}</th></tr></thead><tbody>@forelse($invoices as $invoice)<tr><td>{{ $invoice->invoice_number }}</td><td>{{ $invoice->order?->order_number }}</td><td>{{ $invoice->order?->buyer?->company_name }}</td><td>{{ $invoice->issue_date?->format('d M Y') }}</td><td>{{ $invoice->due_date?->format('d M Y') ?: '-' }}</td><td>{{ $invoice->currency }} {{ number_format((float) $invoice->total_amount, 2) }}</td><td>{{ number_format((float) $invoice->paid_amount, 2) }}</td><td><span class="zBadge zBadge-complete">{{ __(ucwords(str_replace('_', ' ', $invoice->status))) }}</span></td><td><button class="primary-btn" data-bs-toggle="modal" data-bs-target="#payment-modal-{{ $invoice->id }}">{{ __('Payment') }}</button> @if((float) $invoice->total_amount > (float) $invoice->paid_amount && $gateways->contains(fn($gateway) => $gateway->currencies->contains('currency', $invoice->currency)))<button class="primary-btn" data-bs-toggle="modal" data-bs-target="#gateway-payment-modal-{{ $invoice->id }}">{{ __('Online Payment') }}</button>@endif <a class="primary-btn" href="{{ route('admin.garments.invoices.edit', $invoice->id) }}">{{ __('Edit') }}</a> <a class="primary-btn" target="_blank" href="{{ route('admin.garments.invoices.print', $invoice->id) }}">{{ __('Print') }}</a><form method="post" action="{{ route('admin.garments.invoices.destroy', $invoice->id) }}" class="d-inline">@csrf @method('delete')<button class="primary-btn" type="submit" onclick="return confirm('{{ __('Delete this invoice?') }}')">{{ __('Delete') }}</button></form></td></tr>@empty<tr><td colspan="9" class="text-center">{{ __('No invoices found.') }}</td></tr>@endforelse</tbody></table></div></div>
@foreach($invoices as $invoice)<div class="modal fade zModalTwo" id="payment-modal-{{ $invoice->id }}" tabindex="-1"><div class="modal-dialog modal-md modal-dialog-centered"><div class="modal-content zModalTwo-content"><form method="post" action="{{ route('admin.garments.invoices.payments.store', $invoice->id) }}" class="p-4">@csrf<h4 class="mb-3">{{ __('Record Payment') }} - {{ $invoice->invoice_number }}</h4><p>{{ __('Outstanding') }}: {{ $invoice->currency }} {{ number_format(max(0, (float) $invoice->total_amount - (float) $invoice->paid_amount), 2) }}</p><div class="mb-3"><label>{{ __('Payment Date') }}</label><input type="date" name="payment_date" class="form-control" value="{{ now()->toDateString() }}" required></div><div class="mb-3"><label>{{ __('Amount') }}</label><input type="number" name="amount" class="form-control" step="0.0001" min="0.0001" max="{{ max(0, (float) $invoice->total_amount - (float) $invoice->paid_amount) }}" required></div><div class="mb-3"><label>{{ __('Method') }}</label><select name="payment_method" class="form-control" required><option value="bank_transfer">{{ __('Bank Transfer') }}</option><option value="lc">{{ __('L/C') }}</option><option value="cash">{{ __('Cash') }}</option><option value="other">{{ __('Other') }}</option></select></div><div class="mb-3"><label>{{ __('Reference') }}</label><input name="reference" class="form-control"></div><button class="primary-btn" type="submit">{{ __('Save Payment') }}</button></form></div></div></div>@endforeach
@foreach($invoices as $invoice)<div class="modal fade zModalTwo" id="gateway-payment-modal-{{ $invoice->id }}" tabindex="-1"><div class="modal-dialog modal-md modal-dialog-centered"><div class="modal-content zModalTwo-content"><form method="post" action="{{ route('admin.garments.invoices.payments.checkout', $invoice->id) }}" class="p-4">@csrf<h4 class="mb-3">{{ __('Online Invoice Payment') }} - {{ $invoice->invoice_number }}</h4><p>{{ __('Outstanding') }}: {{ $invoice->currency }} {{ number_format(max(0, (float) $invoice->total_amount - (float) $invoice->paid_amount), 2) }}</p><div class="mb-3"><label>{{ __('Gateway') }}</label><select name="gateway" class="form-control" required>@forelse($gateways->filter(fn($gateway) => $gateway->currencies->contains('currency', $invoice->currency)) as $gateway)<option value="{{ $gateway->slug }}">{{ $gateway->title }} ({{ $invoice->currency }})</option>@empty<option value="" disabled>{{ __('No configured gateway supports this invoice currency') }}</option>@endforelse</select></div><div class="mb-3"><label>{{ __('Amount') }}</label><input type="number" name="amount" class="form-control" step="0.0001" min="0.0001" max="{{ max(0, (float) $invoice->total_amount - (float) $invoice->paid_amount) }}" value="{{ max(0, (float) $invoice->total_amount - (float) $invoice->paid_amount) }}" required></div><button class="primary-btn" type="submit">{{ __('Continue to Payment') }}</button></form></div></div></div>@endforeach
<div class="modal fade zModalTwo" id="invoice-modal" tabindex="-1"><div class="modal-dialog modal-lg modal-dialog-centered modal-dialog-scrollable"><div class="modal-content zModalTwo-content"><form method="post" action="{{ route('admin.garments.invoices.store') }}" class="p-4">@csrf<h4 class="mb-4">{{ __('Create Commercial Invoice') }}</h4><div class="row g-3"><div class="col-md-6"><label>{{ __('Order') }}</label><select name="order_id" class="form-control" required><option value="">{{ __('Select order') }}</option>@foreach($orders as $order)<option value="{{ $order->id }}">{{ $order->order_number }} - {{ $order->buyer?->company_name }}</option>@endforeach</select></div><div class="col-md-6"><label>{{ __('Invoice Number') }}</label><input name="invoice_number" class="form-control" required></div><div class="col-md-4"><label>{{ __('Issue Date') }}</label><input type="date" name="issue_date" class="form-control" value="{{ now()->toDateString() }}" required></div><div class="col-md-4"><label>{{ __('Due Date') }}</label><input type="date" name="due_date" class="form-control"></div><div class="col-md-4"><label>{{ __('Currency') }}</label><input name="currency" class="form-control" value="USD" required></div><div class="col-md-4"><label>{{ __('Amount') }}</label><input type="number" step="0.0001" min="0" name="amount" class="form-control" required></div><div class="col-md-4"><label>{{ __('Tax') }}</label><input type="number" step="0.0001" min="0" name="tax_amount" class="form-control" value="0"></div><div class="col-md-4"><label>{{ __('Paid Amount') }}</label><input type="number" step="0.0001" min="0" name="paid_amount" class="form-control" value="0"></div><div class="col-md-6"><label>{{ __('Status') }}</label><select name="status" class="form-control"><option value="issued">{{ __('Issued') }}</option><option value="draft">{{ __('Draft') }}</option><option value="partially_paid">{{ __('Partially Paid') }}</option><option value="paid">{{ __('Paid') }}</option></select></div><div class="col-12"><label>{{ __('Notes') }}</label><textarea name="notes" class="form-control" rows="3"></textarea></div></div><div class="text-end mt-4"><button class="primary-btn" type="submit">{{ __('Save Invoice') }}</button></div></form></div></div></div>
@endsection

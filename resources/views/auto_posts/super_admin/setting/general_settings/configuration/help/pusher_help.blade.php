<div class="text-center py-4">
    <h4 class="fs-20 fw-600 mb-3">{{ __('Pusher credentials status') }}</h4>
    <hr class="my-4">
    <p class="fs-16 mb-4">{{__('If you enable this. The system will enable for pusher')}}</p>
    <a class="primary-btn" target="_blank"
       href="{{ route('super_admin.setting.configuration-settings') }}">{{ __('Back to configuration settings') }}</a>
</div>
